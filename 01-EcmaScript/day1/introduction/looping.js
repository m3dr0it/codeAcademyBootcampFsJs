var s = "js fullstack";

for (let i = 0; i < s.length; i++) {
    console.log(s[i]);
}

let x = 0;
console.log(Boolean(x));
while (x<5) {
    if (x === 3) {
        console.log(x);
        break
    }
    console.log(x);
    x++
}